{
  "name": "timeslot-finder",
  "version": "1.0.0",
  "description": "Chrome Extension for finding free time slots in Microsoft 365 Outlook",
  "private": true,
  "scripts": {
    "build": "web-ext build --source-dir . --artifacts-dir dist --ignore-files node_modules dist .github pinuts.md README.md package.json package-lock.json",
    "lint": "web-ext lint --source-dir .",
    "test": "bun test"
  },
  "devDependencies": {
    "chrome-types": "latest",
    "web-ext": "^10.4.0"
  }
}
