name: Build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    uses: ./.github/workflows/lint.yml

  build:
    name: wxt zip
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Build extension ZIP
        run: bun run build:zip

      - name: Prepare artifact
        run: cp .output/timeslot-finder-*.zip timeslot-finder.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: timeslot-finder-${{ github.sha }}
          path: timeslot-finder.zip
          retention-days: 30
